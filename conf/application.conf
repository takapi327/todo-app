# https://www.playframework.com/documentation/latest/Configuration

slick.dbs.default{
  driver = "slick.driver.MySQLDriver$",
  db.profile = "com.mysql.Driver",
  db.url = "jdbc:mysql://localhost/Scala",
  db.user = "root",
  db.password = "transamGN001"
}